# Simple Account LRU cache implementation using skip list and hash table

## Requirements
- Java 17
- Maven (for JUnit 5 testing library)

## Running the tests
- Windows:
```shell
PS > ./mvnw clean test
```

- Linux shell (bash, zsh, ...), macOS terminal:
```shell
$ ./mvnw clean test
```
